name: trading_chart_flutter
description: "Pure-Dart candlestick trading chart widget for Flutter, with custom RenderObject pipeline, Lightweight Charts-style API, and TradingView-like gestures."
version: 0.1.0
homepage: https://github.com/nvsces/trading_chart_flutter
repository: https://github.com/nvsces/trading_chart_flutter
issue_tracker: https://github.com/nvsces/trading_chart_flutter/issues

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0